# Terraform Modules

Currently active modules:

- `vm/` - reusable Proxmox VM module.

Add new modules only when there is real shared Terraform logic to extract, for example network, storage, or Kubernetes resources.

---
- name: Install Docker repo dependencies
  ansible.builtin.dnf:
    name:
      - dnf-plugins-core
    state: present

- name: Add Docker CE repository
  ansible.builtin.command: >
    dnf config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
  args:
    creates: /etc/yum.repos.d/docker-ce.repo

- name: Import Docker GPG key
  ansible.builtin.command: rpm --import https://download.docker.com/linux/centos/gpg
  register: docker_gpg_import
  until: docker_gpg_import.rc == 0
  retries: 5
  delay: 10

- name: Install Docker CE and compose plugin
  ansible.builtin.dnf:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    state: present

- name: Ensure Docker service is enabled and running
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Add homelab user to docker group
  ansible.builtin.user:
    name: homelab
    groups: docker
    append: true
  notify: Restart docker

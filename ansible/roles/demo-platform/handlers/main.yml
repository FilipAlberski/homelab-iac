---
- name: Restart demo platform
  ansible.builtin.command: docker compose up -d --force-recreate
  args:
    chdir: "{{ demo_platform_dir }}"
  changed_when: true

- name: Reload systemd for demo cleanup
  ansible.builtin.systemd:
    daemon_reload: true
